namespace Lua
{
    public struct DefaultLuaCode
    {
        public const string code = @"
        -- Called once the execute code button is run
        -- Do not remove this function
        function onEnabled()

        end

        -- Called once code execution is stopped
        -- Do not remove this function
        function onDisabled()
            
        end

        -- Called once a frame
        -- Do not remove this function
        function onUpdate(delta)

        end
        ";
    }
}